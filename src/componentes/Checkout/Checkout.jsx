import "bootstrap/dist/css/bootstrap.css"
import { cartContext } from "../../storage/cartContext"
import { useEffect, useContext } from "react"

export default function Checkout({nombre, precio, cantidad}) {
    return (
        /* Items con cantidades
        Total de la compra
        Input para nombre, apellido y telefono
        Input para email y logica de repetir el email 2 veces?

        Finalizada la orden, debo recibir mi orden id con el id del objeto de firebase */
        <div>
            
        </div>
    )
}